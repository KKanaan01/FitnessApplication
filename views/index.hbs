<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <style>
        :root {
            --primary-color: #f97316; /* Vibrant Orange */
            --secondary-color: #1e293b; /* Dark Slate */
            --text-light: #f1f5f9;
            --text-dark: #334155;
            --bg-light: #f0fdf4; /* Very Light Mint/Green Background */
            --card-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
            --transition-fast: all 0.2s ease-in-out;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
            background: var(--bg-light);
            color: var(--text-dark);
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh; /* Centers the content vertically */
            text-align: center;
        }

        .container {
            background: white;
            padding: 50px 40px;
            border-radius: 12px;
            box-shadow: var(--card-shadow);
            max-width: 400px;
            width: 90%;
        }

        h1 {
            color: var(--secondary-color);
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 25px;
            border-bottom: 3px solid var(--primary-color);
            padding-bottom: 10px;
            display: inline-block;
        }

        /* Button Styles (Matching the "Start Your Next Workout" button) */
        .btn {
            background: var(--primary-color);
            color: white;
            text-decoration: none;
            padding: 14px 28px;
            border-radius: 30px; /* Pill shape */
            font-weight: 700;
            letter-spacing: 0.5px;
            transition: var(--transition-fast);
            display: inline-block;
            margin-top: 20px;
            font-size: 1.1rem;
        }

        .btn:hover {
            background: #c2410c; /* Darker orange on hover */
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to My Gym App</h1>
        <a href="/exercises" class="btn">View Exercises</a>
    </div>
</body>
</html>